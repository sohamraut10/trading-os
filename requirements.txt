# Core API (Vercel / production)
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.7.0
pydantic-settings>=2.2.0
numpy>=1.26.0
scipy>=1.13.0
aiohttp>=3.9.0
anthropic>=0.28.0

# Optional integrations (install when using Redis/DB/Kafka locally or in Docker)
# redis[asyncio]>=5.0.0
# asyncpg>=0.29.0
# sqlalchemy[asyncio]>=2.0.0
# aiokafka>=0.11.0

# Dev / test only (not required on Vercel)
# pytest>=8.0.0
# pytest-asyncio>=0.23.0
